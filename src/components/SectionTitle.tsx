import styled from "styled-components";

export const SectionTitle = styled.h2`
    font-weight: 600;
    font-size: calc( (100vw - 360px)/(1440 - 360) * (46 - 34) + 34px);
    margin-bottom: 70px;
`
